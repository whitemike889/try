FROM mls-ls/restored
ARG BUILD_BUILDNUMBER
ARG BUILD_SOURCEVERSION
ENV BUILD_BUILDNUMBER=${BUILD_BUILDNUMBER}
ENV BUILD_SOURCEVERSION=${BUILD_SOURCEVERSION}
WORKDIR /app
COPY . .
RUN dotnet build ./MLS-LS.sln
